// Export default PostCSS configuration object for CSS processing pipeline
export default {
  // Define PostCSS plugins to be used during CSS compilation
  plugins: {
    // Tailwind CSS plugin for utility-first CSS framework processing
    tailwindcss: {},
    // Autoprefixer plugin for automatic vendor prefix addition to CSS properties
    autoprefixer: {},
  },
};
